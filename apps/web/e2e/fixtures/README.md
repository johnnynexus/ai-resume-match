Place a small **text-based** sample resume PDF here as `sample-resume.pdf` for the
Playwright happy-path test (`e2e/analysis.spec.ts`). It must contain extractable
text (not a scan), since the API rejects image-only PDFs.
